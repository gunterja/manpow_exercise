<h1>Listing Blogs</h1>

<ul>
  <% @blogs.each do |blog| %>
    <li><%= blog.blog_name %> -- <%= link_to "View", blog %></li>
  <% end %>
</ul>

<%= link_to "My Posts", blog_path(current_user.id), class: 'btn btn-primary' %>
